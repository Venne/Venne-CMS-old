<h1>{_"Error module"}</h1>

<ul class="menu">

	<li n:if="$presenter->isAllowed('Error:Default:default')">
		<a href="{link default}"{if $presenter->isCurrent("Error:Default:default")} class="active"{/if}>list</a>
	</li>

	<li n:if="$presenter->isAllowed('Error:Default:create')">
		<a href="{link create}"{if $presenter->isCurrent("Error:Default:create")} class="active"{/if}>new item</a>
	</li>

</ul>
