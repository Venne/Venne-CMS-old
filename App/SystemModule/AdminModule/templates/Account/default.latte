{block content}

{include "../head.latte"}

<script type="text/javascript">
    jQuery(document).ready(function() {

		function setVisibility(){
			if(!$("#frmformEdit-use").is(":checked")){
				$("#frmformEdit-name").attr('disabled', 'disabled');
				$("#frmformEdit-password").attr('disabled', 'disabled');
				$("#frmformEdit-password_confirm").attr('disabled', 'disabled');
			}else{
				$("#frmformEdit-name").removeAttr('disabled');
				$("#frmformEdit-password").removeAttr('disabled');
				$("#frmformEdit-password_confirm").removeAttr('disabled');
			}
		}

		if($("#frmformEdit-section").val() != "common"){
			$("#frmformEdit-use").change(function(){
				setVisibility();
			})
			setVisibility();
		}

    });
</script>

{control formEdit}
